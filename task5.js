let dataTypes = ['string', 'boolean', 'null', 'undefined', 'number', 'object', 'symbol'];
for (let i = 0; i < dataTypes.length; i++) {
    alert(dataTypes[i]);
};

let i = 0;
while (i <= 10) {
    alert(i += 2);
};